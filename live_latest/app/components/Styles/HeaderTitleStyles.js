import { StyleSheet } from 'react-native'
import {
    Colors,
    Fonts,
} from '@resources'

export default StyleSheet.create({
    title: {
        ...Fonts.style.semiBold_18,
        color: Colors.white,
        textAlign:'center'
    },
})
